//! `pirust-orchestrator` — supervises headless `pirust --mode rpc` workers.
//!
//! pirust port of `packages/orchestrator`. See `docs/analysis/04-orchestrator.md`.
//! Supervisor + JSONL socket protocol land in feat-009.

fn main() {
    eprintln!(
        "pirust-orchestrator: not yet implemented (scaffold). See docs/analysis/04-orchestrator.md"
    );
    std::process::exit(2);
}
